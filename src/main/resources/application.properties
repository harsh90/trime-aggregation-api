# disable beans endpoint
#management.endpoints.beans.enabled=false
# expose all endpoints:
#management.endpoints.web.exposure.include=*
#management.info.git.mode=full
# Url where Admin Server is running
spring.boot.admin.client.url=https://demosb-admin-server.herokuapp.com

spring.boot.admin.client.instance.service-url=https://trimi-service.herokuapp.com

# Expose all the Actuator endpoints
management.endpoints.web.exposure.include=*
# Show details in Health check section
management.endpoint.health.show-details=always
 If you don't set this, username 'user' will be used by default
# And a password will be auto-generated each time your app starts
# Such password is visible in the console during app startup
spring.security.user.name=harshitha
spring.security.user.password=my-secret-password

# Provide username and password for Spring Boot Admin Server
# To connect to the client
spring.boot.admin.client.instance.metadata.user.name=harshitha
spring.boot.admin.client.instance.metadata.user.password=my-secret-password


# Credentials to authenticate with the Admin Server
spring.boot.admin.client.username=harshitha
spring.boot.admin.client.password=1qaz2wsx@

spring.application.name=trimi-service

demo.test=test property name
